{
  "name": "techy-toppers-root",
  "version": "1.0.0",
  "description": "Root package to run backend and frontend concurrently",
  "scripts": {
    "setup-db": "powershell -ExecutionPolicy Bypass -File setup_db.ps1",
    "start-backend": "powershell -ExecutionPolicy Bypass -File start_backend.ps1",
    "start-frontend": "npm run dev --prefix frontend",
    "dev": "npm run setup-db && concurrently \"npm run start-backend\" \"npm run start-frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
